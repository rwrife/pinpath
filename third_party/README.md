# Third-party dependencies

- `pico-sdk` — Raspberry Pi Pico SDK pinned at tag `2.0.0` (`efe2103f9b28458a1615ff096054479743ade236`) as a git submodule.

Initialize after clone:

```bash
git submodule update --init --recursive
```
